name: Repository Health

on:

  schedule:

    - cron: "0 5 * * 1"

jobs:

  audit:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - run: python tools/repository_auditor.py